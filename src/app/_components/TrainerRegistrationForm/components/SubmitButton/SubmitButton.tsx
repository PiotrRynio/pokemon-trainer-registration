import { BaseButton } from "@/components/Buttons/BaseButton";

type SubmitButtonProps = {};

export const SubmitButton = ({}: SubmitButtonProps) => {
  return <BaseButton type="submit">Submit</BaseButton>;
};
