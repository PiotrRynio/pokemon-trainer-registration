import { colors } from "@/styles/common/colors";

export const shadows = {
  focused: `0 0 0 4px ${colors.primaryLight}`, // check in mockup and colors.ts
  dialog: "0 4px 10px 2px rgba(0, 0, 0, 0.1)",
};
